* Add update method for reimb status
* Change all the error messages
* Implement logs
* implement testing
* Change all the ids to unique ids
* Make sure the printstack traces are in the right palces
* Look at password encryptions